#  API layer 
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# Stage 1: LinkedIn extraction (direct-fetch demo path)
httpx>=0.27
beautifulsoup4>=4.12

# Stage 2: autonomous navigation
browser-use>=0.1
anthropic>=0.40
# browser-use pulls in playwright automatically.
# After installing, you MUST also fetch the browser binary:
#   playwright install chromium